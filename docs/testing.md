# Testing

## Running Tests

To run the test suite for Katana, execute the following command in the repository root:

```shell
pnpm test
```

## Testing Strategy

The test suite validates the behavior of all stack combinators, ensuring they manipulate the stack correctly based on function arity and position. Developers should run tests locally before submitting any contributions.
